"use client";

// ... (all previous AdminPaymentsPage logic, but as a function PaymentsPageClient({ userName, userRole }) and using those props in MainLayout)

// Accepts: userName: string, userRole: string
// Usage: <MainLayout userRole={userRole} userName={userName}> ... 