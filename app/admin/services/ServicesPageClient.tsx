"use client";

// ... (all previous AdminServicesPage logic, but as a function ServicesPageClient({ userName, userRole }) and using those props in MainLayout)

// Accepts: userName: string, userRole: string
// Usage: <MainLayout userRole={userRole} userName={userName}> ...
