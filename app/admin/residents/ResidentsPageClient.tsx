"use client";

// ... (all previous ResidentsPage logic, but as a function ResidentsPageClient({ userName, userRole }) and using those props in MainLayout)

// Accepts: userName: string, userRole: string
// Usage: <MainLayout userRole={userRole} userName={userName}> ...
