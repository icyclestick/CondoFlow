"use client";

// ... (all previous AdminGatepassPage logic, but as a function GatepassPageClient({ userName, userRole }) and using those props in MainLayout)

// Accepts: userName: string, userRole: string
// Usage: <MainLayout userRole={userRole} userName={userName}> ...
