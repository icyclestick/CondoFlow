"use client";

// ... (all previous UnitsPage logic, but as a function UnitsPageClient({ userName, userRole }) and using those props in MainLayout)

// Accepts: userName: string, userRole: string
// Usage: <MainLayout userRole={userRole} userName={userName}> ...
